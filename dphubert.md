1.在第一步中，学生模型从教师模型初始化，并进行联合蒸馏和剪枝，以生成具有预定大小的更小模型。在第二步中，已经剪枝的学生模型进一步进行蒸馏以提升性能。在这两个步骤中，仅使用未标记的语音数据，且教师模型保持冻结。

2.Step‑1 蒸馏损失的梯度，承担了传统剪枝里 “敏感度打分” 的角色，用来判断哪些模块可以删

3.Step‑1 训练时学生**代码结构、张量大小和教师完全一样，只是前向靠随机掩码临时屏蔽部分通路，没有真正删参数**；真正把模型做小，是 Step‑1 训练完成之后，依据学到的 α 做一次物理裁剪，产出小模型，之后交给 Step‑2 蒸馏打磨权重

4.# DPHuBERT 方法总结：对比单纯剪枝、单纯蒸馏，优势 & 可改进点

## 一、和单纯剪枝、单纯蒸馏核心差异

### 1）单纯结构化后剪枝（传统方案：训完大模型 → 离线算敏感度 → 删掉模块 → finetune）

- 流程：大模型完整训练完毕，**训练和剪枝是两个独立阶段**；剪枝依靠启发式指标：权重幅值、Fisher、Taylor 等。
- 评判依据：模型自身权重 / 下游任务损失；**没有老师模型做知识对齐**。
- 缺点：
  1. 敏感度是静态快照，剪完权重一变，重要性评估失效；
  2. 大多基于下游任务 loss 打分，剪出来的网络偏向特定任务，通用预训练基座多任务能力下降；
  3. 剪完后需要单独微调恢复性能，容易恢复不回来，掉点严重。

### 2）单纯知识蒸馏（例如 DistilHuBERT）

- 流程：**人工预先设计好小模型结构**（多少层、头数、FFN 维度写死）；用大老师做层对层蒸馏，训练这个手工设计的学生小模型。
- 关键点：网络结构是人定死的，训练过程网络结构不会变化。
- 缺点：
  1. 极度依赖人工结构设计，需要大量实验调参找合适网络尺寸；很难找到最优结构；
  2. 人工均匀裁剪往往没有挖掘模型真实冗余；
  3. 部分方案训练开销巨大（LightHuBERT）。

### 3）DPHuBERT（联合蒸馏 + 结构化剪枝，两步流程）

1. Step‑1：**学生初始和老师结构完全一致**，使用 Hard‑Concrete 重参数化，**训练过程中蒸馏损失直接作为剪枝敏感度信号，联合稀疏约束（增广拉格朗日）在线学习哪些模块要被屏蔽**；训练阶段只是软掩码屏蔽，不物理删权重。
2. Step‑1 结束：依据学到的 α，**物理裁剪得到自动生成的非均匀稠密小模型**（图 2 的异构网络）。
3. Step‑2：结构完全固定，移除掩码，继续蒸馏对齐老师表征，修复剪枝带来的性能损失。

> 
> 核心区别一句话：
> 
> 
> - 单纯剪枝：先训模型，后剪；剪枝评判不靠蒸馏；结构是剪完才变小。
> - 单纯蒸馏：人工提前定小模型结构，训练不改结构。
> - DPHuBERT：用蒸馏损失在线充当剪枝的敏感度标尺，**训练中自动搜索网络结构，不需要人工设计学生骨架**；再二次蒸馏打磨剪后模型。

## 二、DPHuBERT 的优势

1. **不需要人工设计小模型网络结构**
不用手动试多少注意力头、FFN 维度、CNN 通道；由蒸馏信号 + 稀疏约束自动搜索出异构网络结构，省去大量结构搜索实验。对比 DistilHuBERT 这类人工定义学生结构的蒸馏，降低人力调参成本。
2. **剪枝的评判标准面向通用预训练表征，而非下游任务**
剪枝的敏感度来自**对齐老师中间表征的蒸馏损失**，不是下游任务标签损失。因此剪出来的基座是通用的，在 SUPERB 十项语音任务综合表现更好，不会为单个任务过拟合。传统任务导向剪枝会出现 “ASR 很好，说话人识别崩掉”。
3. **联合优化，而非 “先蒸馏后剪枝 / 先剪枝后蒸馏” 的串行流水线**
传统串行：蒸馏完再剪枝，剪枝会破坏蒸馏学到的知识；DPHuBERT 在训练时就知道 “删掉模块会怎么破坏老师表征对齐”，优先舍弃对知识迁移不重要的组件，性能损失更小。
4. **训练成本可控，对数据量友好**
对比 LightHuBERT 等重搜索的蒸馏方案，GPU 开销更低；只用 100h 少量无标签音频，压缩后的模型也能拿到不错效果。
5. **输出稠密可直接部署的模型**
是结构化剪枝，输出普通稠密 Transformer，不需要稀疏推理库，可直接跑推理框架，工程落地友好。
6. **两步式策略（step1 联合剪枝蒸馏 + step2 纯蒸馏修复）**
消融证明第二步蒸馏修复必不可少，专门补偿联合优化阶段为了满足稀疏约束带来的表征损伤。

## 三、论文方法现存不足 / 可以改进的方向

> 
> 来自论文讨论部分 + 从工程科研视角的拓展点

### 1）训练阶段显存开销大（硬痛点）

Step‑1 训练时学生维持完整老师的全部张量，只是软掩码屏蔽部分通路；**训练显存接近原始大模型，并没有因为剪枝而节省训练显存**。

> 
> 改进方向：
> ① 探索渐进式物理剪枝：训练过程中就真正删掉部分模块，降低训练显存；
> ② 结合蒸馏 + 稀疏训练，降低训练阶段资源消耗，方便大模型（HuBERT‑Large）压缩。

### 2）压缩上限有限，极低参数量场景性能衰减明显

论文实验观察：当压缩到非常小（极高稀疏度），ASR 这类序列建模任务掉点比分类任务严重。序列任务对网络容量更敏感。

> 
> 改进方向：针对不同任务类型做差异化稀疏约束；或者引入任务感知的联合蒸馏剪枝，在基座阶段就适度考虑下游任务信号。

### 3）稀疏目标是全局参数量约束，不是 FLOPs 约束

现在增广拉格朗日约束的是**参数量占比**，不是推理 FLOPs。参数量和 FLOPs 不完全等价；有些结构参数量低，但计算量依然很高。想要直接控制推理速度，需要把约束改成 FLOPs 而不是参数量。

### 4）目前只支持无标签语音做基座压缩，没有引入下游任务监督

整个 step1 完全没有下游任务信号，得到通用基座。虽然多任务平均好，但在某一个特定下游任务上，不一定比 “面向该任务做剪枝” 效果最优。

> 
> 改进方向：混合蒸馏损失 + 少量下游任务损失，做到兼顾通用性和特定任务性能。

### 5）Hard‑Concrete 超参调优负担

Hard‑Concrete 分布本身有若干超参（温度、边界等），加上增广拉格朗日的超参，超参空间比较大；不同基座（HuBERT‑Large/WavLM）需要重新调参。

### 6）没有和其他结构搜索 NAS 做充分对比

本质上这是一种基于蒸馏的网络结构搜索；论文主要对比蒸馏、后剪枝，没有和语音领域 NAS 做对标。

### 7）暂未考虑动态推理

输出是固定静态结构；不能做到动态：不同样本用不同网络规模。