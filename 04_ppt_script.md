# 文献调研 PPT 文案：剪枝 + 蒸馏联合压缩

> 覆盖 5 篇：DPHuBERT、Amber-Image、PPCL、NanoFLUX、Bridging
> 四篇 diffusion 工作细节据 talking.md（已逐篇核对 arXiv）；DPHuBERT 据 01/02 文档 + 公开描述，细粒度机制标注"需回原文确认"。

---

## Slide 0｜封面

**标题**：剪枝 + 蒸馏联合压缩：从语音 SSL 到 Diffusion Transformer 的技术路线调研

**口播**：各位好，本次调研围绕一个共同问题——"先删掉模型里可能不重要的部分，再用更大的教师模型教被压缩的学生恢复能力"。我选了五篇有代表性的工作，横跨语音自监督模型和图像/视频扩散 Transformer，重点看它们**什么时候剪、怎么剪、什么时候蒸、蒸什么、为什么这么设计**。

---

## Slide 1｜问题与统一视角

**核心问题**：如何在尽量保持生成/表征质量的前提下，同时降低参数量、计算量、采样步数与设备延迟。

**统一范式**：

```
教师 T（完整大模型，冻结）
   ↓  结构剪枝（删层 / 删通道 / 删 head / 删 block）
学生 S（更小，可训练）
   ↓  蒸馏恢复（中间特征 / 噪声预测 / 速度场 / 生成分布）
恢复后的紧凑模型
```

**五篇的差异轴**（预告）：
- 剪什么：层 / 连续层 / attention head / block / 通道 / token
- 蒸馏时机：剪后蒸 / 剪蒸交织 / 逐级蒸 / 剪后先修复再一步蒸
- 蒸馏目标：hidden state / 噪声预测 / velocity / 生成分布

**口播**：这五篇都可装进"剪枝 + 蒸馏"这个框架，但每篇在三个轴上选择不同，造就了完全不同的技术路线。下面逐篇看。

---

## Slide 2｜五篇定位图

| 论文 | 领域 / 模型 | 剪枝对象 | 蒸馏时机 | 蒸馏目标 | 是否步数蒸馏 |
|---|---|---|---|---|---|
| **DPHuBERT** | 语音 SSL / HuBERT | FFN + attention 结构 | **剪蒸同循环（联合）** | 多层 hidden state | 否 |
| **Amber-Image** | 文生图 / Qwen-Image MMDiT | 深度层 + 双流转单流 | 每次结构变化后 | hidden state + 扩散 loss | 否 |
| **PPCL** | 文生图 / Qwen-Image、FLUX DiT | 连续层 + text/FFN 宽度 | 剪枝中同步 | 连续层输出 + 中间特征 | 否 |
| **NanoFLUX** | 文生图 / FLUX.1-Schnell | head / 维度 / block / token / T5 | 逐级压缩后 | flow/velocity + 文本/视觉表示 | 否（沿用已有步数蒸馏） |
| **Bridging** | 类条件图 / EDM2-XS | U-Net block / 通道 | **剪后先 repair 再一步蒸** | 高噪声 latent 上教师 denoising → 生成分布 | **是（一步 SiDA）** |

**口播**：这张表是全场纲领。注意 Bridging 是唯一真正把"步数蒸馏"接进来的，其余四篇的蒸馏都是"恢复"语义，不是少步加速——这也呼应了 diffusion 文献里"蒸馏"一词被占用的辨析。

---

## Slide 3｜DPHuBERT：联合而非分阶段

**针对模型**：HuBERT（自监督语音表征模型）

**技术路线**（核心特征：剪枝决策与 KD 在**同一个训练循环**内联合优化，而非 prune-then-finetune）：

- **教师**：完整 HuBERT（冻结）
- **学生**：剪枝后的 HuBERT
- **剪枝**：对 transformer block 的 FFN 与 attention 做结构化剪枝，用可学习掩码决定保留哪些维度，L1 正则促稀疏
- **蒸馏**：多层 hidden-state 对齐（不仅最终输出，含中间层）
- **联合损失**：自监督掩码预测目标 + 蒸馏对齐损失 + 掩码稀疏正则，三项一起优化

```
完整 HuBERT 教师（冻结）
   ↓
学生 = 剪枝 HuBERT（带可学习掩码）
   ↓  同一循环内同时：
   ├── 蒸馏：对齐教师多层 hidden state
   ├── 剪枝：L1 推动掩码稀疏
   └── 自监督：保留 HuBERT 原训练目标
   ↓
紧凑 HuBERT
```

**Benchmark**：SUPERB（语音多任务：ASR、说话人、情感、意图等）

**关键点 / 为什么联合**：
- 蒸馏信号反过来**引导哪些结构该剪**，剪枝又逼学生把容量集中到重要维度；
- 联合 > "先剪后蒸"或"先蒸后剪"的分阶段管线。

> ⚠️ DPHuBERT 的具体掩码机制与损失权重需回原文（arXiv:2305.17651, INTERSPEECH 2023）确认；本页细粒度部分依据公开描述整理。

**口播**：DPHuBERT 是五篇里唯一标题就写"Joint"的，也是唯一非扩散的。它的核心主张是：剪枝和蒸馏不要分两步做，放在一个循环里，让蒸馏信号指导剪枝决策。后面四篇扩散工作大多是分阶段的，DPHuBERT 是个反衬。

---

## Slide 4｜Amber-Image：MMDiT 的层剪与双流转单流

**针对模型**：Qwen-Image（约 20B，60 层双流 MMDiT），产出 Amber-Image-10B 与 6B 两个版本。

**技术路线**：

**第一阶段 10B：时间步敏感的深度剪枝**
- 逐层消融：暂时关掉第 l 层，比较原始与删层后的噪声预测差异
- 跨多个时间步加权累积（高噪声阶段权重更高——决定全局布局/语义）
- 重要性低 → 删除，60 层删 30 层
- **局部权重平均初始化**：保留层用"自身 + 后面连续被删层"的教师权重平均来初始化

```
W_l^init = (1/(k+1)) Σ_{j=0}^{k} W_{l+j}^orig
```

- 蒸馏分两步：①只训新初始化层，对齐教师区间末端 hidden state；②解冻全参数，用标准扩散 loss 微调

**第二阶段 6B：后 20 层双流 → 单流**
- 前 10 层保留双流（需保留模态独立性），后 20 层改共享单流（文本/图像已融合，双流冗余）
- 单流层用 image branch 权重初始化
- 教师目标 = 拼接(text hidden, image hidden)，学生单流用 MSE 拟合

**完整流程**：
```
Qwen-Image 20B / 60层双流
  → 时间步敏感层消融 → 删30层
  → 局部权重平均初始化
  → 层级蒸馏 → 全参数微调  = Amber-Image-10B
  → 后20层双流转单流 + image初始化
  → 隐藏状态蒸馏 + 轻量微调 = Amber-Image-6B
```

**Benchmark**：DPG-Bench、GenEval、OneIG-Bench（通用）；LongText-Bench、CVTG-2K（文字渲染）。全流程约 1872 GPU 小时，参数最多降约 70%。

**关键点**：
- "局部权重平均"是用**教师**相邻层权重初始化**学生**保留层（不是训练中平均师生）；
- 教师目标是教师在相同输入下的**中间 hidden state**，不是新标签；
- 前双流后单流 = 按模态融合程度分而治之。

**口播**：Amber-Image 的亮点有两个。一是时间步敏感的层重要性——它不在单一噪声水平判重要性，而是按"高噪声管布局、低噪声管细节"加权。二是局部权重平均：删掉连续几层后，让保留层用这几层教师权重的平均来初始化，给学生一个比复制或随机更好的起点，再分两步蒸馏——先只训新层对齐教师 hidden state，再全参数微调。

---

## Slide 5｜PPCL：连续冗余层 + 可插拔压缩比例

**针对模型**：Qwen-Image、FLUX.1-dev、FLUX.1 Lite 等 MMDiT/DiT。

**核心思想**：不按单层 importance 排序，而是找**连续冗余区间**——相邻层功能耦合，连续删比随机删更易通过蒸馏恢复。

**技术路线**：

**①连续冗余区间检测**
- 给每层训练轻量 linear probe，用 probe 替换连续层构造 surrogate
- 计算 CKA 相似度及其一阶差分 Δ
- Δ 先降后升的拐点 = 冗余区间终点 v

**②深度剪枝 + 非顺序蒸馏**
- 删 [u,v] 区间，学生层用教师第 u 层初始化，直接拟合教师第 v 层表示
- 非顺序蒸馏：每个区间单独训，避免误差逐层累积

**③宽度剪枝**
- text stream：保留 QKV，其余换轻量线性投影
- FFN：用小线性投影替代
- 蒸馏同时匹配最终输出和被替代的中间表示

**④全参数微调 → 按需插回部分教师层得到 14B/12B/10B 等不同规模**

**Benchmark**：DPG、GenEval、OneIG-Bench、T2I-CompBench、LongText-Bench；效率指标参数量/GPU 显存/延迟。Qwen-Image 20B→14B 平均性能仅降约 0.42%。

**关键点**：
- "Pluggable"：同一框架内重插教师层即得不同压缩比，免重新设计；
- 非顺序蒸馏解决层间误差累积；
- 50% 参数压缩下关键指标下降 < 3%。

**口播**：PPCL 和 Amber-Image 都剪 DiT 的层，但 PPCL 强调"连续层"而非单层。它用 linear probe + CKA 一阶差分自动找冗余区间，再做非顺序蒸馏——让一个学生层直接学教师连续多层的总效果，而不是逐层对齐，避免误差一层层传下去。另一个卖点是可插拔：插回几层教师就能换出不同大小模型，不用为每个压缩比重训。

---

## Slide 6｜NanoFLUX：逐级渐进压缩到移动端

**针对模型**：FLUX.1-Schnell（约 17B pipeline：12B DiT + 5B T5-XXL）→ NanoFLUX 约 2.4B（2B DiT + 330M T5-Large），目标移动端。

**技术路线**：每压缩一级蒸一次，逐级降低师生能力差距。

```
12B → 5B   剪 attention heads (24→16, hidden 3072→2048)，SVD 定冗余 head
 5B → 3B   剪每个 head 特征维度 (128→96)，SVD
 3B → 2.5B 删/合并冗余 block（按输入输出余弦相似度）
2.5B → 1.8B 静态化 AdaLN（系数方差小，预计算）
   + token downsampling（ResNet，T→T/4，仅高噪声时间步用低分辨率）
   + 文本编码器蒸馏 T5-XXL→T5-Large（借去噪器早期视觉 hidden state 辅助）
```

**蒸馏损失**（每级统一）：
```
L_distill = ‖ f_S(x_t,t,p) − f_T(x_t,t,p) ‖²    （flow/velocity prediction）
```

**师生关系（关键）**：
- 5B 学 12B；3B 先学 5B 再由 12B 校正；2.5B 先学 3B 再由 12B 校正
- 远距离教师（上一级）管易收敛，全局教师（12B）管最终质量

**Benchmark**：OneIG-Bench、DPG-Bench、GenEval、HPDv3/HPSv3、Samsung S25 Ultra 真实移动端延迟。移动端 512×512 约 2.5 秒/图。

**关键点**：
- 不只剪 transformer，还压 token 数（O(T²) attention）和文本编码器；
- 时间步自适应 token 分辨率：高噪声低分辨率、低噪声高分辨率保纹理文字；
- 取舍明确：愿意牺牲一定组合性/文字质量换移动端延迟。

**口播**：NanoFLUX 最像"逐级缩小"。12B 不直接教 2B——能力差太大训不动，所以 12→5→3→2.5→1.8B 一级一级蒸，每级用上一级当近距离教师、12B 当全局校正。它还顺手把 token 数和 T5 文本编码器也压了，因为这三者都是移动端瓶颈。它不追最低 FID，追的是"手机上可接受质量下的低延迟"。

---

## Slide 7｜Bridging：剪枝与一步蒸馏之间的"修复桥"

**针对模型**：EDM2-XS，ImageNet-512 类条件生成。目标：把多步教师压成 1 NFE 一步生成器。

**核心发现**：剪枝模型直接做一步 SiDA 蒸馏会**失败**（FID 112.56）——剪枝后学生的 denoising field 已偏离教师，不在有效蒸馏区域，梯度不可靠，样本崩溃。

**技术路线**：

```
完整 EDM2-XS 教师 (124.7M, 63 NFE, FID 3.53)
  → 真实 ImageNet latent 加噪 (σ*=2.5)
  → 计算每个 block 的 teacher-alignment sensitivity I(b)
       I(b) = ε(G^{-b}) − ε(G)   （删 b 后与教师误差 − 删前误差）
  → 删低敏感性 branch/channel/block
  → 切片复制教师权重 → 剪枝模型 G0
  → 【关键】Teacher-aligned repair：
       在真实 noisy latent (σ_r=σ_init=2.5) 上
       L_repair = ‖ G_S(z̃,σ_r,y) − T(z̃,σ_r,y) ‖²
       只把学生拉回教师附近，不重训成多步模型
  → 以 repaired 权重初始化 SiDA 一步蒸馏
  → 1 NFE 一步生成器 G*
```

**Benchmark / 结果**：
| 模型 | 参数 | NFE | FID |
|---|---|---|---|
| EDM2-XS 教师 | 124.7M | 63 | 3.53 |
| 20% 剪 + repair + SiDA | 98.8M | 1 | **3.12** |
| 30% 剪 + repair + SiDA | 88.0M | 1 | 4.26 |
| 直接 SiDA 无 repair | — | 1 | 112.56 |

**关键点**：
- repair **不是**重新训练一个多步扩散模型，而是极短的过渡，专对齐一步生成的高噪声起点；
- 真正贡献是发现并填补"剪枝学生虽结构变小、但还没进入可一步蒸馏的参数区域"这个鸿沟；
- repair bridge 原则上可接其他一步蒸馏损失。

**口播**：Bridging 是五篇里最特殊的一篇，因为它把"步数蒸馏"接了进来。它发现一个关键现象：剪完枝直接做一步蒸馏会彻底崩——FID 112。原因是剪枝学生还没进入教师可蒸馏的区域。解法很轻：在真实图像加噪的高噪声点上，用 MSE 把学生短暂拉到教师附近，再用 SiDA 做一步蒸馏。repair 不是重训，是"过桥"。结果 20% 剪枝后反而比原教师 FID 还低，且只需 1 次网络调用。

---

## Slide 8｜横向对比

| 维度 | DPHuBERT | Amber-Image | PPCL | NanoFLUX | Bridging |
|---|---|---|---|---|---|
| 什么时候剪 | 联合循环 | 先剪层再双流转单流 | 深度再宽度 | 逐级 | 先剪 block/channel |
| 怎么剪 | 可学习掩码+L1（待确认） | 时间步敏感层消融+局部权重平均 | CKA一阶差分找连续区间 | SVD+余弦相似度+静态化 | 教师对齐误差 I(b) |
| 何时蒸馏 | 与剪枝同循环 | 每次结构变化后 | 剪枝中同步 | 每级压缩后 | 剪后先repair再一步蒸 |
| 蒸馏什么 | 多层 hidden state | hidden state + 扩散 loss | 连续层输出+中间特征 | flow/velocity + 文本/视觉表示 | 高噪声latent教师denoising→生成分布 |
| 步数蒸馏 | 否 | 否 | 否 | 否 | **是** |
| 师生关系 | 固定教师 | 固定教师 | 固定教师 | **逐级教师链** | 固定教师 |
| 评测 | SUPERB | DPG/GenEval/LongText | DPG/GenEval/OneIG | OneIG/DPG/GenEval/HPS | ImageNet-512 FID |

**口播**：一张表看完差异。两个最值得记的分化：一是师生关系——四篇用固定教师，NanoFLUX 用逐级教师链；二是蒸馏语义——只有 Bridging 的蒸馏是"步数加速"，其余都是"恢复"。

---

## Slide 9｜技术路线演进与共性洞察

**演进脉络**：
1. 静态删 U-Net block/layer（SnapFusion、BK-SDM）
2. 自动层搜索 + 宽度/深度联合剪枝（PPCL、Amber-Image）
3. 按输入/时间步动态执行（PARE、Dynamic-in-Few-Step）
4. 结构压缩 + 步数蒸馏 + 教师对齐 + 移动端引擎统一（NanoFLUX、Bridging）

**共性结论**：
- 几乎都"先剪后蒸"或"剪蒸交织"，**唯一真正联合单循环的是 DPHuBERT**；
- 蒸馏目标普遍落在**中间特征 / 速度场**对齐，而非只对最终输出；
- 剪枝依据越来越"任务感知"：时间步敏感、CKA、教师对齐误差——纯按权值幅度已少见；
- diffusion 里"蒸馏"≠"步数蒸馏"：恢复类蒸馏（Amber/PPCL/NanoFLUX）与步数蒸馏（Bridging）是两件事，Bridging 正是研究二者的衔接。

**口播**：把五篇放回更大脉络，剪枝依据在从"按权值大小"走向"任务/教师感知"——时间步敏感性、CKA、教师对齐误差。而蒸馏目标几乎都对齐中间特征或速度场，不只看最终输出。还有一个反复出现的主题：剪枝和步数蒸馏能不能直接拼——Bridging 给出的答案是"中间必须有个对齐修复"。

---

## Slide 10｜对本课题（DiT/FLUX 剪枝+蒸馏）的启示

1. **剪枝依据要任务感知**：Amber-Image 的时间步加权、PPCL 的 CKA 连续区间、Bridging 的教师对齐误差都可直接借鉴到 FLUX 剪枝。
2. **非顺序蒸馏防误差累积**：PPCL 的"学生一层学教师连续多层"适合 DiT 层剪后的恢复。
3. **逐级教师链**：若从 FLUX 大幅压缩，NanoFLUX 的逐级蒸馏比一步到位更稳。
4. **若要做少步/一步生成**：必须像 Bridging 那样在剪枝与步数蒸馏之间插教师对齐 repair，否则会崩。
5. **benchmark 锚定**：DPG-Bench + GenEval + OneIG-Bench 是新一代 DiT 文生图压缩的主流三件套；移动端再加真实设备延迟。

**口播**：落到我们自己的课题。如果只做剪枝后恢复，可借鉴 Amber-Image 和 PPCL 的特征蒸馏；如果要把 FLUX 压到很小，NanoFLUX 的逐级教师链更稳；如果还想接步数蒸馏做加速，Bridging 已经证明中间必须有个 repair 桥，不能直连。benchmark 上，DPG、GenEval、OneIG 是新一代 DiT 压缩的事实三件套。以上是本次调研，谢谢。



一句话
剪枝和蒸馏联合 = 把"删结构"和"教恢复"放在同一个训练循环里一起优化，而不是先剪完再蒸的分阶段管线。

关键区别在"同时" vs "先后"：


分阶段（非联合）：   剪枝（定死结构）→ 蒸馏恢复 → 微调
联合：             剪枝决策 ↔ 蒸馏信号  在同一循环内互相影响
分阶段里，剪枝时还不知道哪些结构"好恢复"，是盲剪；联合里，蒸馏的梯度反过来告诉剪枝"哪些该删、删了能补回来"。

联合比单纯剪枝/蒸馏的优势
对比单纯剪枝
单纯剪枝（Han / Wanda / SparseGPT 那类）剪完要么不恢复、要么只做普通 SGD 微调，性能掉得多；联合用教师蒸馏恢复，质量损失小。
单纯剪枝的剪枝依据是"权值幅度 / 激活统计"等静态指标，盲剪；联合里剪枝依据变成"删掉后教师能不能补回来"这种任务/教师感知指标，剪得更准。
对比单纯蒸馏
单纯蒸馏（不剪枝）只把大模型知识蒸给一个预设好结构的小模型，结构是拍脑袋定的；联合让结构本身也被优化，学生不是"随便选的小模型"而是"被蒸馏信号塑造出的小模型"，容量分配更合理。
单纯蒸馏压不到那么小——蒸馏只换知识不删参数，要真省参数和 FLOPs 还是得剪枝。
联合自身的核心优势（三者相较）
剪枝有指导：蒸馏梯度反过来引导哪些结构冗余，避免盲删重要通道。
恢复有锚点：剪枝后的学生立刻有教师信号兜底，不会偏离太远再拉回，比"剪崩了再蒸"省力。
结构更优：最终结构是"剪蒸共同塑造"的，比手设结构的容量-质量比更高。
一次训练搞定：免去"剪→蒸→再剪→再蒸"的多轮成本。
一个最直白的例子
DPHuBERT 就是标题写明"Joint"的代表：剪枝掩码的 L1 稀疏正则 + 教师 hidden-state 蒸馏 + 自监督目标，三项放一个 loss 一起优化。它比"先剪 HuBERT 再用教师蒸"的分阶段管线掉点更少——因为蒸馏信号在剪枝过程中就一直在告诉模型"这部分容量留着有用、那部分删了能补"。

注意：这五篇里只有 DPHuBERT 是真正单循环联合；Amber-Image/PPCL 是"剪蒸交织但分步"，NanoFLUX 是"逐级剪-蒸"，Bridging 是"剪→repair→一步蒸"。它们都叫"剪枝+蒸馏"，但严格意义上联合程度不同——真联合只有 DPHuBERT。

如果我主要是说剪枝，那这个时候蒸馏是不是就变成了一种恢复手段？和我全量训练恢复是不是一样都是一种恢复手段？那还有什么别的手段？
是的——如果主语是剪枝，蒸馏就是一种恢复手段
把剪枝当成主问题，恢复手段是一整类方法的集合，蒸馏只是其中一种。你和"全量训练恢复"并列，是对的。区别只在用什么信号把剪掉的性能拉回来：

恢复手段	信号来源	典型代表
普通全量微调/重训	标准任务损失 + 硬标签（CE / flow-matching）	Han Deep Compression、NetAdapt、LLM-Pruner 的 LoRA
蒸馏恢复	教师的软输出（logit / 中间特征 / 速度场）	Amber-Image、PPCL、NanoFLUX、BK-SDM
全量训练 vs 蒸馏：都是恢复，本质差别在监督密度
全量训练恢复：用任务本身的损失（生成任务的 CE/flow loss、分类的 CE）。监督是"稀疏"的——只有一个最终目标信号，剪枝后的学生要从这个远端信号自己摸索回正确解，剪枝幅度大时容易训崩或掉点多。
蒸馏恢复：在任务损失之外，额外引入教师模型在每个中间层、每个软标签上的输出。监督是"密集"的——学生不仅知道"最终要对"，还知道"中间每一层该长什么样"，所以恢复得更快、更稳、掉点更少，尤其在大幅剪枝时。
一句话：全量训练是"只给最终答案让学生自己悟"，蒸馏是"教师全程手把手带"。两者都是恢复，但蒸馏是更密集的恢复信号。这也是你 02 文档的核心结论——经典剪枝-重训谱系（Han/NetAdapt/Lottery Ticket）几乎全是普通 SGD 微调、不用 KD；KD 是另一条独立谱系。

还有哪些别的恢复手段（你 01/02 文档已覆盖）
把"恢复"理解宽一点，不只是"训回去"，还有这些：

权重补偿（不训练） — SparseGPT、OWL：用近似逆 Hessian 把删掉连接的输出分摊到剩余权重上，一步补偿、不开训。
不恢复 — Wanda：按 |W|·‖X‖ 剪完就上线，靠剪枝度量本身准，不做任何更新。
结构保留 — SliceGPT：用正交/PCA 找低方差维度切片，靠结构本身不掉性能，后接少量微调。
LoRA 微调 — LLM-Pruner、LoRAPrune：只训低秩适配器，便宜但恢复力有限。
持续预训练 + 数据策略 — Sheared LLaMA：targeted 结构化剪枝 + Dynamic Batch Loading + 教师数据配比上继续预训练，不是经典 KD loss（常被误归入剪枝+蒸馏，实为剪枝+继续预训练）。
prompting 式恢复 — Compresso：collaborative prompting 让 dense 模型在剪枝全程当 soft teacher，非经典 logit KD。
靠更好度量减少恢复负担 — Pruner-Zero：进化搜索 symbolic 度量，剪得更准 → 不用怎么恢复。
自蒸馏（无外部教师） — EPSD、SDMPrune：用剪枝前的自己当教师，省一个独立 teacher。
落到你课题的选择逻辑
剪枝幅度小 → 普通微调/LoRA 够用，便宜；
剪枝幅度大 → 蒸馏恢复更稳（密集监督）；
想极致省事 → 权重补偿（SparseGPT 式，不训练）；
你的 FLUX 剪枝要保生成质量 → 蒸馏（对齐中间特征/速度场）明显优于纯微调，这也是 Amber-Image/PPCL/NanoFLUX 全都选蒸馏恢复的原因。

所以"蒸馏是一种恢复手段"这个理解完全正确，它和全量训练是同层并列关系，区别只在信号密度；而恢复手段还有"不训练补偿""结构保留""继续预训练"等多种，构成一个谱系。