1. 从12B蒸馏到2.4B，有点牛，但是是不是不具备泛化性？

2. 推理步数，容量较低的学生模型受益于更多的推理步数

3. 将注意力头数量H从24均匀减少到16，就从12B到5B了，这里做的端到端蒸馏

4. 为什么这篇文章先剪宽度再剪深度？

5.消融研究：我们将块合并策略与两种基线方法进行比较：(1) 一次性剪枝（Prune）整个冗余块序列；(2) 迭代剪枝（IterPrune），一次剪除一个块。表 7 表明，合并冗余块的参数始终能获得最佳结果。我们进一步观察到，将剪枝和合并操作应用于 Single-Stream DiT 块时效果更佳，而通过修改 MMDiT 块来获得 2.5B 模型则会导致明显的性能下降。表 4 还报告了 3B 模型与先前压缩阶段模型的定量比较，表明我们的块合并策略能在保持生成质量的同时有效减少模型深度。